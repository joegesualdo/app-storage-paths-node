{
  "name": "@joegesualdo/app-storage-paths-node",
  "repository": "joegesualdo/app-storage-paths-node",
  "version": "0.0.1",
  "description": "Get paths for all your apps storage (i.e. config, data, etc)",
  "main": "./dist/index.js",
  "scripts": {
    "test": "npm run build && ./node_modules/ava/cli.js -v test.js",
    "build": "./node_modules/distify-cli/cli.js --input-file=./index.js --output-dir=./dist --is-node",
    "prepublish": "npm run build"
  },
  "devDependencies": {
    "ava": "^0.15.2",
    "distify-cli": "0.0.10"
  },
  "dependencies": {},
  "keywords": [
    "paths",
    "config",
    "cache",
    "temp",
    "storage"
  ],
  "author": {}
}
